> Fictional demonstration scenario generated by CaseReady 1.0.1. No real customer, organisation, account or incident is represented.

Scenario: **Domain shows a certificate warning**

# Internal support brief

## The portal domain presents a certificate-name warning after its DNS record was changed.

Nadia at Harborlight Learning (fictional) is trying to open the learner portal so the afternoon enrolment session can begin.

### Customer outcome and impact

- Desired outcome: restore trusted access to the portal and confirm what the customer should expect while DNS changes propagate
- Business impact: all demonstration users see a browser certificate warning and cannot safely continue to the portal
- Scope: All users or a whole service
- Time pressure: Work cannot continue
- Safe workaround: Not available
- Severity: S1 — critical interruption

### Observed behaviour

- Expected: The portal loads over HTTPS with a trusted certificate for the portal hostname.
- Actual: The browser blocks the page because the certificate does not include the requested hostname.
- Frequency: Every attempt from both test networks
- Status: Unavailable

### Timeline and context

- First seen: 10:18 UTC today
- Last known working: The portal loaded normally at 09:42 UTC today
- Changed immediately before: The DNS CNAME was changed from the old hosting target to the new edge target at 09:55 UTC.
- Environment: Demonstration domain, Chrome 128 and Safari 18, two test networks

### Reproduction

- Open the demonstration portal URL in a private browser window from either test network.
- Message: NET::ERR_CERT_COMMON_NAME_INVALID

### Work already completed

- Tried: Confirmed the current DNS answer; checked the certificate hostname; tested from a second network; did not bypass the browser warning.
- Result: Both networks resolve to the new target. The served certificate still names the provider hostname rather than the portal hostname.
- Evidence: DNS lookup output from both networks; certificate subject and SAN capture; timestamped browser screenshots.

### Ownership and commitment

- Owner: Theo in Technical Support
- Customer promise: provide the next status update in 30 minutes and advise whether the enrolment session needs an alternative
- Next update: 10:55 UTC today

### Handoff

- Next team: Hosting and DNS provider
- What exactly we need: Confirm custom-hostname activation for the demonstration domain, issue the matching certificate, and provide a safe rollback point if activation cannot complete before the customer update.

---

Reference CASE-DEMO-3174 · Suggested severity remains an operator decision.
